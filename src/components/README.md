# components

## Purpose

Shared React components: the reward-mechanic visualizations (flame, plant,
cup, wheel, scratch, dots, points bar), general-purpose UI (back button,
image uploader, pro-lock, reward celebration), app-wide providers, and the
`landing/` and `ui/` subtrees.

## Contents

- `back-button.dom.test.tsx` — jsdom test: `BackButton` renders its label as a link with the given `href`
- `back-button.tsx` — `BackButton`: styled "leave this page" ghost button wrapping a `next/link`, mirrors qkit's identical component
- `card-burst.dom.test.tsx` — jsdom tests: `CardBurst` renders nothing when inactive; renders 40 absolutely (not fixed) positioned particles when active
- `card-burst.tsx` — `CardBurst`: client component rendering a container-relative fireworks-style particle burst (40 randomized-size pieces, mixed square/circle shapes, wider spread), used inside `RewardCelebration`
- `card-shell.dom.test.tsx` — jsdom tests: `CardShell` renders children, tilts on `pointermove`, resets to flat on `pointerleave`, skips the tilt entirely under `prefers-reduced-motion`
- `card-shell.tsx` — `CardShell`: shared outer wrapper for every card-type view (`PreviewCard`/`ProgramCardStatus`) giving all 8 card styles a uniform treatment — a capped (±6°) pointer-tracking 3D tilt via `perspective`/`rotateX`/`rotateY` — with one shared change instead of eight; pure CSS transforms + a light pointer handler, deliberately not three.js/Framer Motion (see `docs/superpowers/specs/2026-07-25-loyalty-card-animation-polish-design.md`); tilt is never engaged (no listeners attached) under reduced-motion, not just visually suppressed; deliberately does NOT set `transform-style: preserve-3d` — combining that with this element's own `overflow-hidden` is a documented rendering conflict that caused the card to visibly distort/"expand" whenever `CardBurst` was active during a tilt. Previously also had an idle `conic-gradient` sheen drifting across the card — removed per user feedback ("irritating arrow-like shadow moving left and right"), it read as an ugly artifact, not a holographic shine.
- `color-picker.dom.test.tsx` — jsdom tests: `ColorPicker`'s trigger swatch reflects the current `value` as its background color, and uses `label` as its accessible name (the popover's internal `react-colorful` drag interaction isn't exercised — same "no dedicated test" precedent as `@merqo/ui`'s own `InfoTooltip`, a similar small Popover-composed component)
- `color-picker.tsx` — `ColorPicker`: a shadcn-composed color picker (`ui/popover.tsx` + `react-colorful`'s `HexColorPicker`/`HexColorInput`) — shadcn/ui has no official color-picker primitive to `npx shadcn add`, so this composes a real shadcn primitive with a small headless library for the one piece shadcn doesn't ship, instead of a native `<input type="color">` (which hands the whole picker UI to the OS/browser with zero styling control); used by `setup-form.tsx`'s Wheel/Scratch segment editor
- `cup.dom.test.tsx` — jsdom tests: `Cup` renders an svg, the coffee ellipse is present but zero-sized at Empty, grows past Empty, always sets an explicit `cx`, the saucer/pedestal ellipses are present even when empty, the tulip-pour flourish only at the Full stage (fading+scaling in on mount instead of popping), the shared 1600ms growth-transition duration on the coffee ellipse, the fixed coffee palette shifting color across fill stages, and wilting forcing all three coffee gradient stops to the muted-foreground color
- `cup.tsx` — `Cup`: 3/4-perspective SVG "looking down into a mug" progress visualization for plant-variant "cup" programs — a hand-drawn shell (saucer, pedestal, two-tone handle, gradient-filled body, elliptical rim, a static translucent rim-shadow ring) that never itself scales/moves, plus ONE persistent coffee `<ellipse>` (`data-cup-coffee`) whose `cy`/`rx`/`ry` are set directly per stage from a shared-bottom-edge layout (`COFFEE_BOTTOM`/`COFFEE_CY`, all fill stages anchored to Full's real bottom edge) so the surface genuinely rises/widens in place rather than one shape swapping for another; its radial-gradient fill has three `<stop>`s (`data-cup-coffee-stop`) recolored per stage so the espresso→latte tone blends continuously. A pour-stream mark (stage 1) and a hand-traced pitcher pouring milk (stage 3, `data-cup-tulip`'s sibling flourishes) grow in via `transform-box:fill-box` scale/opacity; the tulip latte-art flourish (`data-cup-tulip`) still fades+scales in only at the Full stage. Cup shell + coffee both use a fixed, non-brand-themed palette (`LIQUID_COLORS`-successor `COFFEE_TONES` plus the shell's own cream/tan gradients), all animated over the shared 1600ms `motion-safe:` transition; wilting mixes the shell 55% toward muted-foreground and forces the coffee stops fully to it.
- `dashboard-tour.dom.test.tsx` — jsdom tests confirming loopkit wires the right props through to `@merqo/ui`'s `DashboardTour`: a lazy steps resolver (mobile-vs-desktop via `matchMedia`, resolved at tour-start time not render time), `seen` passed through unchanged, `isHomeRoute` true only on `/dashboard`, `navigateHome` pushing to `/dashboard`, `onFirstSeen` posting to `/api/tour-seen` with `keepalive: true` (and never throwing even if that fetch itself rejects), and `scopeClassName="loopkit-tour"` — the tour mechanism itself (driver.js lifecycle, auto-run/replay timing, mark-seen-once semantics, popover styling) is owned and tested by the shared package
- `dashboard-tour.tsx` — `DashboardTour({ seen })`: thin wiring around `@merqo/ui`'s `DashboardTour` — supplies loopkit's own step content (`tour-steps.ts`, resolved lazily via `matchMedia` at tour-start time), a `markTourSeen` mark-seen callback, and `usePathname`/`useRouter`-derived home-route/navigation callbacks; the tour mechanism itself (driver.js overlay lifecycle, auto-run/replay timing, floating "?" button, popover styling) lives in the shared package. `markTourSeen` is a plain `fetch("/api/tour-seen", { method: "POST", keepalive: true })`, not a Server Action: `@merqo/ui`'s shared `DashboardNav` renders its links as plain `<a>` tags (no Next.js dependency), so every dashboard nav click — including the one that brings a vendor back to the overview page while the just-auto-started tour is still up — is a hard navigation, and only `keepalive` (unavailable on a Server Action's own internal fetch) guarantees the write survives a hard nav landing mid-flight; see `src/app/api/tour-seen/`
- `elevated-card.tsx` — `ElevatedCard`: shared polished-card primitive (rounded corners, soft two-layer lifted shadow, renders as `div`, `section`, or `li`) used app-wide (dashboard sub-pages, admin console, auth forms, profile/setup); deliberately not qkit's scalloped "kitchen ticket" `Ticket` theme
- `flame-layers.dom.test.tsx` — jsdom tests: `FlameLayers` renders the correct stage label/count, the dim coal dot only at Ember, all 5 hand-drawn stage layers (`data-flame-layer="ember"|"spark"|"small"|"medium"|"large"`) mounted at once with only the active one at full scale, the Full-Campfire→Ember pair transitioning with no delay while Small→Medium→Large carries the old layer's delayed shrink, and motion-safe-gated idle flicker
- `flame-layers.tsx` — `FlameLayers`: hand-built SVG campfire (crossed logs always drawn, plus 5 stage layers — a scattered glowing coal bed at Ember, a coal mound + crackling ghost-flames at Spark, a single candle-shaped lick at Small Fire, a 3-tongue fan at Medium Fire, and the largest single lick with rising sparks + drifting smoke at Full Campfire — each with its own gradient-stop temperature) for stamp-variant "flame" programs, replacing the old stacked-`lucide-react`-icon design. All 5 layers stay permanently mounted and cross-scale via `transform-box:fill-box`: Ember↔Spark and the Full-Campfire→Ember redemption jump use a synchronized crossfade (old shrinks exactly as new grows, no delay), while Small→Medium→Large instead hold the old layer at full size while the new one grows immediately, only shrinking the old one away on a delay once the new one has visibly grown past it — reads as the flame growing taller from within itself rather than two shapes crossing. Idle per-layer flicker/glow-pulse/rising-spark/drifting-wisp/drifting-smoke keyframes (`globals.css`) replace the old single `flame-flicker` keyframe, all `motion-safe:`-gated.
- `landing/`
- `lucky-box.dom.test.tsx` — jsdom tests: renders the mystery-box prompt and pity-progress caption, clamps displayed progress at the pity ceiling
- `lucky-box.tsx` — `LuckyBox`: Lucky Tap's mystery-box "tap for a surprise" visual (Sparkles icon + pity-progress caption) — replaces the generic stamp-dots view Lucky Tap used to share with real stamp/plant programs, now that it's grouped as a Chance-family style alongside Wheel/Scratch; adds an idle `.lucky-box-shimmer` light sweep inviting the tap (its first animation of any kind), disabled under reduced-motion
- `plant.dom.test.tsx` — jsdom tests: `Plant` renders an svg, the stem grows monotonically stage-to-stage, the seed dot and sprout hook curl each show only at their own stage, exactly 5 persistent leaf slots exist with a growing subset visible per stage (0/0/3/4/5), the bud mounts only from Budding onward (its stage-4 fade delayed to match the stem's grow duration), the bloom mounts only at Bloom with leaves still visible, the same `seed` renders an identical bloom across renders while different seeds can render different bloom types, and dims the color when wilting
- `plant.tsx` — `Plant`: SVG growing-plant progress visualization driven by `stage`/`totalStages`/`wilting`/an optional `seed`, restaged with a persistent 5-leaf-slot stem (revealed progressively, never swapped, so leaves don't flash/replace across stages) that keeps growing through all 5 stages instead of freezing after Leafing. At Budding a capsule bud holds at half scale from its own attach point; at Bloom one of 4 approved flower constructions — tulip, daisy, sakura (a 3-blossom cluster), or pansy — grows from that exact same anchor point while the bud only fades away on a delay once the bloom has visibly grown past it, so removal reads as absorbed rather than an independent fade. `seed` (e.g. a stable per-customer key) deterministically picks the bloom type via a simple hash — never `Math.random()`, so a customer's flower stays the same across page reloads instead of flickering; omitted seed defaults to tulip. All growth still runs over the shared 1600ms `motion-safe:` transition; the stem is a fixed-length line animated via a `scaleY` transform (not a resized line, since `x1/y1/x2/y2` aren't CSS-animatable)
- `points-bar.tsx` — `PointsBar`: horizontal progress bar with a "filled / total points" caption, for stamp-variant "points" programs
- `pro-lock.tsx` — `ProLock`: inline pill linking to `/dashboard/plan`, marks a free-tier vendor's Pro-only limit
- `providers.tsx` — `Providers` client component: wraps `children` with a `sonner` `Toaster` (top-right, rich colors), mounted once in the root layout
- `reward-celebration.dom.test.tsx` — jsdom tests: `RewardCelebration` renders the "Reward unlocked!" dialog with a `CardBurst` overlay when open (queried via `screen` since content is portal-rendered), renders nothing when closed
- `reward-celebration.tsx` — `RewardCelebration` client component: `AlertDialog`-based congratulation modal with a `CardBurst` overlay, shows the customer phone and reward text
- `scratch-card.dom.test.tsx` — jsdom tests: renders the cover/prize text, all 3 scratch strokes fully undrawn (`[stroke-dashoffset:100]`) by default, all 3 draw in via the replayable `.scratch-draw-in` keyframe class (not a transition) while `scratching`, staggered per-stroke `animationDelay`s, the whole overlay unmounts once `revealed` (with or without `scratching`), a `.scratch-reveal-shine` sweep once `revealed` and not before
- `scratch-card.tsx` — `ScratchCard`: two-layer card (reward/label content beneath a cover) for scratch-variant programs; the cover + "Scratch to reveal" text are masked by 3 overlapping, randomized-once (same convention as CardBurst's makePieces), staggered "scratched by hand" SVG strokes (`pathLength`-based `stroke-dasharray`, `.scratch-draw-in` keyframe class in `globals.css` — deliberately an `animation`, not a `transition`, since this component remounts fresh every cycle and a `transition` has no prior committed frame to interpolate from on a fresh node, which is why the reveal previously stopped replaying after cycle 1) plus an SVG `feTurbulence`/`feDisplacementMap` filter roughening each stroke's edge into a torn/scratched texture instead of a smooth round-capped line; punches real transparent holes revealing the prize beneath rather than uniformly fading the cover's opacity; plays a one-shot `.scratch-reveal-shine` light sweep across the prize once `revealed` flips true; both disabled under reduced-motion; not canvas (jsdom has no real canvas 2D context, and this repo's other reward-mechanic visuals — Wheel/Plant/Cup — are already SVG for the same testability reason)
- `section.dom.test.tsx` — jsdom tests: `Section` renders its icon/eyebrow/title/description/children, omits the eyebrow paragraph when not provided, renders as a `<section>` element, and wraps content in `ElevatedCard` (not `@merqo/ui`'s own default flat shell)
- `section.tsx` — `Section`: thin adapter delegating header rendering (icon badge, eyebrow, title, description) to `@merqo/ui`'s `Section` via its `wrapper` render-prop, which supplies `ElevatedCard` as the local shell; public shape unchanged, zero call-site changes at either real caller (`profile-form.tsx`, `setup-form.tsx`)
- `social-icons.tsx` — exports `SOCIAL_LINK_FIELDS`: the shared website/Instagram/Facebook/TikTok field list (key, label, icon), using real brand marks in official colors via `@icons-pack/react-simple-icons` (website falls back to a generic `lucide-react` globe)
- `social-links-fields.dom.test.tsx` — jsdom tests: `SocialLinksFields` renders one input per social field prefilled from `value`, adds a key on type, removes a key when its field is cleared
- `social-links-fields.tsx` — `SocialLinksFields` client component: one labeled input per `SOCIAL_LINK_FIELDS` entry, empty string in `onChange` deletes the key rather than storing `""`; shared by the profile page and reused wherever vendor social links are edited
- `stamp-dots.dom.test.tsx` — jsdom tests: `StampDots` renders all dots with the last one as a reward slot, falls back to plain Check/nothing without a mark, renders a preset icon (faded/greyscale on unfilled), renders a photo mark (faded/greyscale on unfilled), and always shows the reward slot's Gift icon regardless of mark
- `stamp-dots.tsx` — `StampDots`: row of dot/gift icons (filled = stamped, dashed = pending, last slot styled as the reward) with a pop animation on the most recently filled dot; optionally renders a preset icon or vendor photo instead of a plain Check, faded/greyscale on unfilled
- `tour-steps.test.ts` — unit tests: `tourSteps` returns 6 desktop / 3 mobile steps, every step anchors to a `data-tour` selector with non-empty title/description, step 1 is always the shop QR block and the last step is always the replay button, mobile swaps the nav-link steps for a single `nav-menu` spotlight, no step title/description contains an em dash, the `.tour-example` preview markup only appears in the first desktop step, and its progress text is the real `stampStrategy.progress()` label (not a hand-copied one)
- `tour-steps.ts` — exports `tourSteps(isMobile)`, the ordered step list (each keyed to a `data-tour="..."` anchor elsewhere in the dashboard): desktop spotlights the shop QR (with an inline `.tour-example` stamp-card preview rendered via the step's `description` HTML — driver.js renders `description` via `innerHTML`, styled by `globals.css`; the progress text itself is the real `stampStrategy.progress()` output from `@/lib/engine/stamp`, not a hand-copied string, so it can't drift — see `../../../docs/superpowers/specs/2026-08-25-tour-example-badge-drift-fix-design.md`), Customers, Activity, Stats, Account, and Replay in turn; mobile collapses the four nav landmarks into a single `nav-menu` spotlight below Tailwind's `sm` breakpoint (driver can't highlight an off-screen element)
- `ui/`
- `wheel.dom.test.tsx` — jsdom tests (with a controllable `requestAnimationFrame`/`performance.now` stub): rotation starts at rest, continuously decelerates during the free-spin phase (each equal-time window covers strictly less distance than the last — not a flat constant speed), lands exactly on the resolved segment's angle continuing forward from wherever the spin currently was (no reset/jump), jumps straight to the resolved angle under reduced motion, reward vs. non-reward segments render in visibly distinct colors, a vendor-picked segment `color` overrides the reward/non-reward default; a `describe("self-owned result overlay")` block covers the win/lose overlay rendering the correct text once settled (for both a reward and non-reward landing), `onSettled` receiving the true result derived from the landed segment (not a separately-passed value), the overlay clearing the instant a new spin starts, and reduced-motion still reflecting the true landed segment
- `wheel.tsx` — `Wheel`: SVG spinning-wheel visualization computing per-segment pie slices and a rotation angle that lands on `landedId`; each segment optionally takes a vendor-picked `color` (hex, via the new `color-picker.tsx`) — white outlined text (`paint-order`+`stroke`, not a real CSS text-shadow which SVG `<text>` lacks) keeps the label legible against any custom color; falls back to emerald (reward) / muted rose (non-reward) when unset, replacing a low-contrast gold/muted-gray pairing that didn't read clearly as win-vs-lose at a glance. Rotation is driven entirely by `requestAnimationFrame`-computed real constant-angular-deceleration ("friction") kinematics — not a CSS transition/animation at all, since a fixed CSS easing curve can't be told "you're moving at exactly this velocity right now, decelerate smoothly from there," which is what produced the "spinning at a constant speed, then suddenly so fast" artifact a two-phase CSS-curve handoff had. The free-spin phase (target segment still unknown, masked by the parent) itself continuously decelerates from the first frame; the moment the target becomes known, a second physics solve reads the _exact_ velocity the free-spin phase had reached and computes a fresh deceleration that brings the wheel to a dead stop precisely on the target angle — continuing the same velocity across the boundary rather than restarting a new curve. Skips the whole simulation and jumps straight to the resolved angle under `prefers-reduced-motion`. **Owns its own win/lose result** — computed directly from the landed segment's `reward` flag the instant it settles, rendered as an overlay directly on the wheel itself (not a separate caller-positioned badge) — replacing an earlier design where the caller tracked a separately-threaded result value that had to stay in sync with this component's own async settle animation, a real synchronization bug surface. Takes an optional `onSettled({won})` callback, fired with that same true result exactly when the settle simulation finishes, purely so callers can gate something else (e.g. `PreviewCard`'s celebration burst, which lives at that level for every card type) on "has the wheel visually finished" — not for the result itself.

## Connectivity

`landing/` composes the marketing sections rendered by `src/app/page.tsx`;
`ui/` is the shadcn/ui primitive layer (`Button`, `AlertDialog`, etc.) that
both this folder's own components (`back-button.tsx`, `reward-celebration.tsx`)
and `landing/` build on. The reward-mechanic visualizations
(`flame-layers.tsx`, `plant.tsx`, `cup.tsx`, `wheel.tsx`, `scratch-card.tsx`,
`stamp-dots.tsx`, `points-bar.tsx`, `lucky-box.tsx`) each render one
`src/lib/engine/` strategy's `ProgressView` variant and are selected by the
dashboard/customer card views based on the program's `type`/`variant`;
`card-shell.tsx` is the shared outer wrapper both card views (`PreviewCard`/
`ProgramCardStatus`) render one of these inside, giving all of them the same
sheen/tilt treatment regardless of which visual they host.

## Parent

[src](../README.md)
