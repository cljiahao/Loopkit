# dashboard

## Purpose

Vendor console root: program grid, shared shop QR, scan-to-serve entry point, and the account nav/layout — plus the server actions (`actions.ts`) that back stamping, engine "visit" events, redemption, card regeneration, and QR-token resolution for the whole dashboard tree.

## Contents

- `actions.ts` — server actions (`stampAction`, `recordVisitAction`, `redeemPlantAction`, `regenerateCardAction`, `resolveTokenAction`, `lookupAction`, `redeemAction`, `saveQkitEarnConfigAction`, `disconnectTelegramAction`) that stamp/play/redeem/regenerate cards, save qkit-earn config, and disconnect a linked Telegram chat, each RLS-scoped via `requireVendor`
- `activity/`
- `card-link.tsx` — client `CardLinkActions`: copy-link and print-QR buttons for the shop QR block
- `card.ts` — exports the shared `StampCard` type `{ id, phone, stamp_count }` used across actions and form components
- `counter/`
- `customers/`
- `dashboard-nav.dom.test.tsx` — jsdom tests asserting `DashboardNav`'s inline nav links, mobile menu toggle, `data-tour` anchors, account-dropdown item order (Switch products/Profile/Settings/Plan/Get help/Feedback/Sign out), that the trigger and the opened dropdown both show the real stall name (never a static "Vendor account" label or the vendor's email, falling back to "Your stall" when unset), the tier badge, that the "Switch products" submenu lists qkit/paykit/stockkit as plain links to their live deployments (never loopkit itself), and that submitting Feedback/Get help calls `submitFeedbackAction`/`submitSupportMessageAction` with the right shape (including the `message`→`body` field mapping) and surfaces a failed submit inline
- `dashboard-nav.tsx` — client `DashboardNav`: composes `@merqo/ui`'s `DashboardNav` (wordmark, Dashboard/Customers/Activity/Stats nav links, mobile burger, `data-tour` anchors) with its internal `AccountMenu` (avatar trigger, Switch products/Profile/Settings/Plan/Get help/Feedback/Sign out dropdown, owning its own Feedback/Get-help Sheet chrome). Passes `LinkComponent={Link}` (`next/link`, `@merqo/ui` v0.10.0+) to `SharedDashboardNav` so its nav links and the `AccountMenu` it composes internally both render as Next.js `Link`s instead of the package's default plain `<a>` — dashboard nav clicks are client-side transitions, not full-page hard navigations (loopkit renders no standalone `AccountMenu`, so this one call site is the only wiring needed). `vendor.subtitle` carries the stall name — `@merqo/ui`'s `AccountMenu` has one subtitle line shared by the trigger and the dropdown header, so unlike the old hand-rolled version there's no separate "Vendor account" descriptor; falls back to "Your stall" when unset. `getHelp`'s `onSubmit` and `onFeedbackSubmit` are throw-adapters around `submitSupportMessageAction`/`submitFeedbackAction`, since those return a `{success,error}` result object but `@merqo/ui`'s contract expects a rejected promise on failure; `isSupportCategory` narrows the sheet's optional `category` string against `SUPPORT_CATEGORY_LABELS`' real keys before it reaches the action, falling back to `"other"`. `switchKits={getSwitchKits("loopkit")}` (`@merqo/ui` v0.14.0+'s `getSwitchKits` helper, backed by its centralized `KIT_FAMILY` registry) resolves the three other live kits (qkit/paykit/stockkit, their `*-sg.vercel.app` URLs) rendered as a "Switch products" submenu — deliberately no live API call or per-vendor filtering (every kit's own dashboard already handles a signed-in vendor without that kit's vendor row gracefully). loopkit no longer hardcodes this list locally (that was `SWITCH_KITS`, added with v0.13.0's `switchKits` prop and removed once v0.14.0 centralized it in `@merqo/ui`); a future new kit only needs `KIT_FAMILY` updated once in `@merqo/ui`, not in every kit's `dashboard-nav.tsx` (see merqo-ui's `docs/superpowers/plans/2026-08-16-kit-switcher.md`)
- `dashboard-page.dom.test.tsx` — jsdom test asserting `DashboardPage` renders a "Your programs" heading above the program grid alongside the Shop QR block and scan entry
- `dashboard-view.test.ts` — unit tests for `shouldShowQr` (hides QR block at zero active programs, shows it otherwise)
- `dashboard-view.ts` — exports `shouldShowQr(activeProgramCount)`, a pure helper deciding whether the shop QR block should render
- `layout.tsx` — async `DashboardLayout` server component: requires vendor auth, redirects admins to `/admin`, renders the sticky header (`px-5 py-3.5`, matching qkit's dashboard-header sizing exactly) with `DashboardNav`, defines an inline sign-out server action, and reads `vendors.tour_seen_at` to render `@/components/dashboard-tour`'s `DashboardTour` (own README), passing `seen={!!vendorRow?.tour_seen_at}`. Wraps `{children}` in the one canonical content container for every `/dashboard` route — `<main className="mx-auto w-full max-w-7xl flex-1 p-5 py-10">` — matching qkit's `dashboard/layout.tsx` pattern; individual pages (`page.tsx`, `loading.tsx`, and every subfolder's `page.tsx`) used to each set their own (inconsistent) `max-w-*`/padding and now render a plain `<div>` inside this `<main>` instead, so the nav's inner `max-w-7xl` row and the page content line up at the same edge. A page whose content genuinely reads better narrower (`profile/`, `counter/`, `plan/`, `settings/`) still nests its own narrower `mx-auto max-w-*` wrapper `<div>` inside this shared container.
- `layout.test.tsx` — asserts the sticky header carries qkit's `px-5 py-3.5 backdrop-blur-md` classes, and that `DashboardTour` receives `seen=false`/`seen=true` depending on `vendors.tour_seen_at`
- `loading.tsx` — `DashboardLoading` skeleton fallback (pulse placeholders for header and cards) shown while the dashboard route segment streams
- `new-program-tile.dom.test.tsx` — jsdom tests for `NewProgramTile` linking to `/setup` when allowed, showing an upgrade prompt at the free-tier cap
- `new-program-tile.tsx` — `NewProgramTile`: trailing grid tile linking to `/setup` to add a program, or a Pro upgrade prompt when at the free-plan cap
- `page.tsx` — `DashboardPage` server component: loads programs, redirects to `/setup` on zero programs, renders the Shop QR block and "Scan a customer" button as a side-by-side quick-actions row (stacks to full-width on mobile) above a "Your programs" heading and the program card grid + new-program tile for active programs; both the populated and empty-state QR/quick-actions blocks carry `data-tour="shop-qr"` — step 1's anchor for the dashboard onboarding tour (`@/components/dashboard-tour`), since this is the first page a vendor lands on
- `plan/`
- `profile/`
- `program-card.dom.test.tsx` — jsdom tests for `ProgramCard` rendering name/badge/description, expiry/head-start detail lines, the Edit link, the whole-card stretched link to the counter page, and that its two links aren't nested inside each other
- `program-card.tsx` — client `ProgramCard`: one card per active program showing type badge, description, detail lines, an Edit link, and a stretched `Link` covering the whole card that opens its counter page (with a decorative chevron affordance) — tapping anywhere on the card opens the counter, replacing the old "Open Counter" button
- `program-display.test.ts` — unit tests for `PROGRAM_TYPE_BADGE`, `describeProgram`, and `programDetails` across every program type
- `program-display.ts` — exports `PROGRAM_TYPE_BADGE` map, `describeProgram()` (one-line reward-mechanic blurb per program type), and `programDetails()` (expiry/head-start detail lines); every branch renders since a vendor's active programs all show at once
- `program-switcher.dom.test.tsx` — jsdom tests for `ProgramSwitcher`: renders "All programs" plus each program, preserves other URL params, hides itself with only one program
- `program-switcher.tsx` — client `ProgramSwitcher`: same-page Select control that switches the `p` query param across Stats/Activity/Customers views, preserving other params; accepts optional `triggerId`/`triggerClassName` to render as a bare standalone control (Customers, Stats — the default) or as one field among others sharing a card's border/background (Activity, via `ActivityFilters`)
- `qkit-earn-settings.dom.test.tsx` — jsdom tests for `QkitEarnSettings`: shows upgrade prompt when not Pro (and that its copy never claims the stamp is awarded automatically), lets a Pro vendor pick a program and toggle the switch
- `qkit-earn-settings.tsx` — client `QkitEarnSettings` form: Pro-gated switch + program picker that calls `saveQkitEarnConfigAction`; the non-Pro upsell copy describes the real claim-link flow (customer taps a link on their qkit order, enters their phone number on `/earn`) rather than overselling it as automatic
- `redeem-button.dom.test.tsx` — jsdom test asserting `RedeemButton`'s confirm dialog shows the exact stamp count and carryover wording
- `redeem-button.tsx` — client `RedeemButton`: AlertDialog-confirmed redeem control that calls `redeemAction` and reports the reset card back to the caller
- `scan-and-route.dom.test.tsx` — jsdom tests for `ScanAndRoute`: passes the "Scan a customer" label through and routes to the resolved card's Counter page with phone pre-filled
- `scan-and-route.tsx` — client `ScanAndRoute`: an `ElevatedCard` wrapping `ScanButton` with a "Scan a customer to stamp or redeem" caption, routing a scanned card to `/dashboard/counter` for its own program with the phone pre-filled
- `scan-button.dom.test.tsx` — jsdom tests for `ScanButton`'s default and custom label rendering
- `scan-button.tsx` — client `ScanButton`: opens a camera modal, decodes a QR via `@zxing/browser`, resolves the token through `resolveTokenAction`, and reports `{ phone, programId }`; `variant` picks the trigger's chrome — `"button"` (default) is the full-width primary trigger used by `scan-and-route.tsx`, `"link"` is a small secondary text trigger for contexts where scanning isn't the primary mechanic (`serve-customer.tsx`'s Counter form)
- `serve-customer.tsx` — client `ServeCustomer`: the full serve flow (stamp/lucky/plant/wheel/scratch) — phone entry is the primary mechanic and renders first (PRODUCT.md's own lead copy is phone-number stamping, not camera scanning), with `ScanButton`'s `"link"` variant ("Scan a QR instead") as a small secondary trigger above the form rather than a full button; primary action + lookup, per-type result rendering (stamp progress, lucky win, plant growth, wheel/scratch), redeem and regenerate-card dialogs, reward celebration
- `settings/`
- `shop-qr-block.dom.test.tsx` — jsdom tests for `ShopQrBlock`'s join copy (named programs vs. generic fallback) and rendered link
- `shop-qr-block.tsx` — `ShopQrBlock`: an `ElevatedCard`-based shared shop-wide QR code panel with join copy naming active programs, the raw link, and `CardLinkActions`; the link-text column uses `self-stretch sm:self-auto` so it's actually width-constrained (and its `truncate` takes effect) in the mobile `flex-col` layout, where the parent's `items-start` alone doesn't stretch flex children to the container width
- `stats/`

## Connectivity

`layout.tsx` wraps every route below (including all 7 subfolders) with vendor-auth enforcement and the `DashboardNav` header; its inline sign-out action posts through the nav's dropdown form. `page.tsx` is the dashboard home, composing `ShopQrBlock` (fed by `card-link.tsx`'s copy/print actions), `ScanAndRoute` (which wraps `scan-button.tsx` and routes into `counter/`), and a grid of `program-card.tsx` cards (styled via `program-display.ts`) plus `new-program-tile.tsx`. `actions.ts` is the shared server-action surface: `serve-customer.tsx` (used by `counter/`) calls `stampAction`/`recordVisitAction`/`lookupAction`/`redeemPlantAction`/`regenerateCardAction`, `redeem-button.tsx` calls `redeemAction`, and `scan-button.tsx` calls `resolveTokenAction`; `qkit-earn-settings.tsx` (used by `settings/`) calls `saveQkitEarnConfigAction`, and `connect-telegram-section.tsx` (also `settings/`) calls `disconnectTelegramAction`. `redeemAction` is also the confirmed live redemption trigger point for Telegram reward-redemption alerts (not `redeemOldestVoucher` in `src/lib/vouchers.ts`, which has no caller anywhere in `src/app/`) — after a successful `redeem` RPC call it does a service-role `vendor_telegram` lookup for the redeeming vendor and fires `sendTelegramMessage` (`@/lib/telegram`) when linked, wrapped so a missing link or a send failure never changes `redeemAction`'s own returned result. `card.ts`'s `StampCard` type is shared by `actions.ts`, `redeem-button.tsx`, and `serve-customer.tsx`. `program-switcher.tsx` is a cross-cutting control reused by the `activity/`, `customers/`, and `stats/` subfolders to filter their merged views by program. `dashboard-view.ts`'s `shouldShowQr` gates whether `page.tsx` renders the QR/scan/grid block or an empty-state message. `layout.tsx` also renders `DashboardTour` (from `src/components/`), which auto-runs once on `page.tsx`'s `[data-tour="shop-qr"]` anchor for a vendor whose `vendors.tour_seen_at` is still null, stamping it as soon as the tour starts (not when it finishes) via a `keepalive` POST to `src/app/api/tour-seen/`.

## Parent

[app](../README.md)
